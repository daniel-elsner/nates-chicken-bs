{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Mode",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/src",
            "env": {
                "AWS_PROFILE": "local-development"
            }
        }
    ]
}