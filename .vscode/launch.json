{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "ncbs - Debug Mode",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/src/main.go",
            "env": {
                "AWS_PROFILE": "local-development"
            }
        }
    ]
}