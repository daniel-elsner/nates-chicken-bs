{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Mode",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}",
            "env": {
                "AWS_PROFILE": "local-development"
            }
        }
    ]
}